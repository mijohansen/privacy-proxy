export const something = () => {
  console.log('sdfasd');
};
/**
 * A table containing extra data connected to a particular url path. We will i
 */
const ingressTableSpec = {
  name: 'ingresses',
  fields: 'ingress,data',
};
